 
            HƯỚNG DẪN: Tự động tạo Unit Test bằng LLM + CI/CD GitHub Actions      Dự án mẫu: DemoUnitTest_ConsoleApp (Console App) + UnitTest (xUnit). Môi trường: .NET 8, Visual Studio 2022, LM Studio 0.3.25, GitHub.         1)      Chuẩn           bị           dự           án       • Tạo solution gồm 2 project:        - DemoUnitTest_ConsoleApp: chứa lớp Calculator và Program.cs        - UnitTest: xUnit test project, tham chiếu (reference) tới DemoUnitTest_ConsoleApp      • Package cho UnitTest.csproj (tham khảo):                &lt;         ItemGroup         &gt;           &lt;         PackageReference          Include="          Microsoft.NET.Test.Sdk          " Version="17.10.0" /&gt;           &lt;         PackageReference          Include="         xunit         " Version="2.9.3" /&gt;           &lt;         PackageReference          Include="         xunit.runner.visualstudio         " Version="3.0.2"&gt;             &lt;         PrivateAssets         &gt;all&lt;/         PrivateAssets         &gt;           &lt;/         PackageReference         &gt;         &lt;/         ItemGroup         &gt;                                                    2) Cài và bật LM Studio (Local LLM)      • Tải model: openai/gpt-oss-20b (hoặc dùng model nhỏ hơn như Qwen2.5-7B-Instruct/Llama-3.1-8B-Instruct).      • Mở tab Developer → Start Server, xác nhận endpoint chạy tại http://localhost:1234.      • Cho phép qua Windows Firewall      khi           được           hỏi     .                                                   3) Program.cs sinh Unit Test tự động      Chương trình đọc Calculator.cs, gọi LM Studio API /v1/chat/completions để sinh mã test và lưu vào UnitTest/UnitTest_Generated.cs.          using System.Text;         using Newtonsoft.Json.Linq;         using System.Net.Http.Headers;                public class Program         {             static async Task Main()             {                 // Tìm file Calculator.cs đi ngược lên từ bin                 string? calculatorPath = FindUpwardFile(AppContext.BaseDirectory, "Calculator.cs");                 if (calculatorPath == null) { Console.WriteLine("Calculator.cs not found"); return; }                 string methodCode = await File.ReadAllTextAsync(calculatorPath, Encoding.UTF8);                        var prompt = $"""Write a real xUnit test for the following C# method.                Do not use Moq. Just call the method and assert the result.                Code:         {methodCode}         """;                        var client = new HttpClient { Timeout = TimeSpan.FromMinutes(6) };                 client.DefaultRequestHeaders.Authorization =                     new AuthenticationHeaderValue("Bearer", "lm-studio");                        var body = new {                     model = "openai/gpt-oss-20b",                     messages = new[] { new { role = "user", content = prompt } },                     max_tokens = 400, stream = false, temperature = 0.2                 };                        var json = System.Text.Json.JsonSerializer.Serialize(body);                 var resp = await client.PostAsync("http://localhost:1234/v1/chat/completions",                                                   new StringContent(json, Encoding.UTF8, "application/json"));                 resp.EnsureSuccessStatusCode();                 var text = await resp.Content.ReadAsStringAsync();                 var raw = JObject.Parse(text)["choices"]![0]!["message"]!["content"]!.ToString();                 string unitTestCode = StripCodeFence(raw);                        var unitTestDir = Path.GetFullPath(Path.Combine(Path.GetDirectoryName(calculatorPath)!, "..", "UnitTest"));                 Directory.CreateDirectory(unitTestDir);                 string outFile = Path.Combine(unitTestDir, "UnitTest_Generated.cs");                 await File.WriteAllTextAsync(outFile, unitTestCode, Encoding.UTF8);                 Console.WriteLine($"Saved: {outFile}");             }                    static string? FindUpwardFile(string start, string name, int max=8){                 var d = new DirectoryInfo(start);                 for(int i=0;i&lt;max &amp;&amp; d!=null;i++, d=d.Parent){                     string c = Path.Combine(d.FullName, name);                     if (File.Exists(c)) return c;                 }                 return null;             }                    static string StripCodeFence(string s){                 if (string.IsNullOrWhiteSpace(s)) return s;                 int a = s.IndexOf("```");                 if (a&gt;=0){                     int b = s.IndexOf("```", a+3);                     if (b&gt;a) s = s.Substring(a+3, b-a-3);                     s = s.Replace("csharp","").Replace("cs","");                 }                 return s.Trim();                    }         }      Lệnh chạy:                    dotnet run --project .\DemoUnitTest_ConsoleApp\DemoUnitTest_ConsoleApp.csproj        dotnet test         4) Đưa mã nguồn lên GitHub      • Khởi tạo Git tại thư mục chứa .sln, tạo commit đầu:                       git init         git add .         git commit -m "Initial commit: console app + xUnit tests + LLM integration"         git branch -M main      • Tạo repository trống trên GitHub (GenAI.Console.LLM), rồi kết nối và đẩy:          git remote add origin https://github.com/&lt;your-username&gt;/GenAI.Console.LLM.git         git push -u origin main      • Hoặc trong Visual Studio: Git → Push to Git service… → chọn GitHub → Create and Push.         5) Bật CI/CD với GitHub Actions (chạy test tự động)       Tạo      file      .     github     /workflows/dotnet-     tests.yml      :              Mở              Developer PowerShell          ngay           tại           thư           mục           gốc      solution (     nơi           có      .     sln     ).        Chạy     :                     #         tạo                 cây                 thư                 mục                 cho         GitHub Actions                      mkdir         -        Force .        github         \workflows                     #         tạo         file         rỗng                      New-Item -Path .        github        \workflows\dotnet-        tests.yml         -ItemType File -Force            Trong VS:         File → Open → File…     →      mở      .\.     github     \workflows\dotnet-     tests.yml           và               dán                   nội          dung workflow          rồi              Save    .             name: CI - .NET Build &amp; Tests         on:           push: { branches: [ main ] }                    pull_request         : { branches: [ main ] }                jobs:           test:             runs-on: ubuntu-latest             steps:               - uses: actions/checkout@v4               - uses: actions/setup-dotnet@v4                 with:                   dotnet-version: '8.0.x'                   cache: false               - name: Restore                 run: dotnet restore DemoUnitTest_ConsoleApp.sln               - name: Build                 run: dotnet build DemoUnitTest_ConsoleApp.sln --configuration Release --no-restore               - name: Test + coverage                 run: &gt;                   dotnet test          UnitTest         /         UnitTest.csproj                    --configuration Release --no-build                   --collect:"         XPlat          Code Coverage"                   --logger "         trx;LogFileName         =test-         results.trx         "               - name: Upload test results                 if: always()                 uses: actions/upload-artifact@v4                 with:                   name: test-results                   path: |                     **/         TestResults         /*.         trx                      **/         TestResults         /**/coverage.cobertura.xml         (Option:      muốn           thấy           trong      Solution Explorer → Right-click         Solution     →         Add → Existing Item…     →      chọn      file      đó           để      “link”      vào      Solution Items.)            Commit &amp; push:                     git add .        github        /workflows/dotnet-        tests.yml                      git commit -m "Add CI workflow to run .NET unit tests"                  git push             Lên      GitHub → tab         Actions     →      thấy      workflow      chạy     .      Sau      khi      commit &amp; push, vào tab Actions sẽ thấy workflow chạy; khi thành công hiển thị dấu ✅ và có artifact test-results (TRX + coverage).                                                    6) Lỗi thường gặp &amp; cách khắc phục      • Không tìm thấy Calculator.cs → dùng FindUpwardFile như trên hoặc kiểm tra đúng đường dẫn/viết hoa-thường.      • Lỗi kết nối 'actively refused' (localhost:1234) → chưa Start Server trong LM Studio hoặc sai port.      • Timeout khi gọi LLM → model 20    G    B      nặng     ;      tăng           HttpClient.Timeout     , giảm max_tokens, hoặc dùng model 7B/8B.      • Git: 'dubious ownership at D:\' → xoá D:\.git; init repo ngay tại thư mục solution.      • Git: Permission denied với .vsidx → thêm .gitignore (bỏ .vs, bin, obj), đóng VS khi git add.      • Actions: packages.lock.json missing → tắt cache: false (như YAML trên) hoặc bật lock file rồi commit.      • Actions: path sai trên Linux (case-sensitive) → đảm bảo đúng tên thư mục/ dự án (UnitTest/UnitTest.csproj).         7) Bước tiếp theo (tuỳ chọn)      • Thêm badge CI vào README.md: ![CI](https://github.com/&lt;user&gt;/GenAI.Console.LLM/actions/workflows/dotnet-tests.yml/badge.svg)       • Bật Branch protection cho main → Require status checks to pass.      • Dùng ReportGenerator để xuất HTML coverage và upload artifact.      • Thêm workflow phát hành (release) nếu cần.           
